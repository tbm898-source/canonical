{
  "profile_id": "canonical.student_handout.v0_1",
  "output_type": "student_handout",
  "allowed_rails": ["aya"],
  "allowed_visibility_scopes": ["aya_classroom", "public_demo"],
  "required_source_fields": ["program_key", "module_key", "session_key", "source_path"],
  "output_contract": {
    "format": "markdown+json",
    "required_sections": ["objective", "student_tasks", "reflection", "submission"]
  },
  "forbidden_patterns": ["prism_private", "owner_only_notes_path", "access_token"],
  "status": "approved",
  "notes": "Student-safe output, no PRISM private language."
}
